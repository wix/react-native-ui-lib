import React from 'react';
import { CharCounterProps } from './types';
declare const CharCounter: {
    ({ maxLength, charCounterStyle, testID }: CharCounterProps): React.JSX.Element | null;
    displayName: string;
};
export default CharCounter;
