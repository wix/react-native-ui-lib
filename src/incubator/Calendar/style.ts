import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  dayContainer: {
    borderWidth: 1,
    width: 32,
    height: 32
  },
  weekNumber: {
    borderWidth: 1,
    width: 24,
    color: 'grey'
  }
});
